interface:
  display_name: "白底图精修"
  short_description: "快速保留透明镜框，按需局部修复镜片，只输出最终图"
  default_prompt: "Use $product-white-background-retouch in final-only mode: preserve complete translucent eyewear edges, lock the source lens colour and transparency, inspect both lenses first, preserve original pixels when they are already clean, repair only specifically identified unnatural reflections through small feathered masks, protect through-lens structures, run a second close-up QA pass after any AI edit, and deliver only one final image."
